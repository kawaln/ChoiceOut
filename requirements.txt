# choiceout.py uses only the Python standard library — no third-party
# dependencies. This file exists because Render's default build step
# (`pip install -r requirements.txt`) requires it to be present.
